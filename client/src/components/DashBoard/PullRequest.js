import { Card, CardHeader, CardContent } from "@mui/material";
export default function PullRequest(){
  return (
    <Card style={{height: "100%"}}>
      <CardHeader title="PullRequest" />
      <CardContent>
      </CardContent>
    </Card>
  );
}
